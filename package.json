{
  "name": "sin-prisa-pero-con-alma",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node scripts/test-admin-data.mjs && node scripts/test-pocketbase-driver.mjs && node scripts/validate-admin-v2.mjs",
    "build": "node scripts/build.mjs",
    "preview": "npx serve dist",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy dist"
  }
}
